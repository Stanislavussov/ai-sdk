# Project Commands

Read `.pi/project.yaml` for available commands.

## Usage

- `run <command>` — execute from config
- `list` — show available commands

## Rules

1. Read `pm` field for package manager
2. Replace `$pm` with actual value
3. If `confirm: true` — ask before running
